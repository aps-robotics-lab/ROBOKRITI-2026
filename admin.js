import{initializeApp}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-app.js';import{getAuth,onAuthStateChanged,signOut}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-auth.js';import{getDatabase,ref,onValue}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-database.js';import{firebaseConfig}from'../firebase/firebase-config.js';const app=initializeApp(firebaseConfig),auth=getAuth(app),db=getDatabase(app);onAuthStateChanged(auth,u=>{if(!u)location.href='admin-login.html';else onValue(ref(db,'registrations'),snap=>{const v=snap.val()||{};const rows=Object.values(v);total.textContent=rows.length;teams.textContent=rows.length;race.textContent=rows.filter(x=>String(x.events||'').includes('Robo Race')).length;war.textContent=rows.filter(x=>String(x.events||'').includes('Robo War')).length;document.querySelector('#table tbody').innerHTML=rows.map(x=>`<tr><td>${x.registrationId||'-'}</td><td>${x.teamName||'-'}</td><td>${x.studentName||'-'}</td><td>${x.class||'-'}</td><td>${Array.isArray(x.events)?x.events.join(', '):x.events||'-'}</td></tr>`).join('')})});logout.onclick=()=>signOut(auth);
