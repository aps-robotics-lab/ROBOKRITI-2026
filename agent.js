import{initializeApp}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-app.js';import{getAuth,onAuthStateChanged,signOut}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-auth.js';import{getDatabase,ref,onValue}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-database.js';import{firebaseConfig}from'../firebase/firebase-config.js';const app=initializeApp(firebaseConfig),auth=getAuth(app),db=getDatabase(app);onAuthStateChanged(auth,u=>{if(!u)location.href='agent-login.html';else onValue(ref(db,'helpTickets'),s=>{const v=s.val()||{};tickets.innerHTML=Object.values(v).map(x=>`<article class="event-card"><b>${x.referenceId||'APS-XXXXX'}</b><h3>${x.subject||'Help Request'}</h3><p>${x.message||''}</p><p>${x.status||'Waiting for Approval'}</p></article>`).join('')})});logout.onclick=()=>signOut(auth);
